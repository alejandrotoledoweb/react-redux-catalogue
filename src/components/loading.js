import React from 'react';

const Loading = () => (
  <div className="loading-bar m-auto text-white" role="status">
    <h2>Loading...</h2>
    <span className="progress" />
  </div>
);

export default Loading;
